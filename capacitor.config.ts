import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ageofempire.android',
  appName: 'ageofempire-android',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
